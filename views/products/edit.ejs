<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <div class="container-xxl">
      <main>
        <h1 class="mt-3">Редактирование продукта</h1>
        <form
          action="/products/edit/<%= product.id %>?_method=PUT"
          method="post"
        >
          <div class="col-xl-6 mt-3">
            <label for="name" class="form-label">Название</label>
            <input
              type="text"
              class="form-control"
              id="name"
              required
              maxlength="50"
              name="name"
              value="<%= product.name %>"
            />
          </div>

          <div class="col-xl-6 mt-3">
            <label for="number" class="form-label">Количество</label>
            <input
              type="number"
              class="form-control"
              id="number"
              required
              name="count"
              value="<%= product.count %>"
            />
          </div>

          <div class="col-xl-6 mt-3">
            <label for="price" class="form-label">Цена за количество</label>
            <input
              type="text"
              class="form-control"
              id="price"
              required
              name="price"
              value="<%= product.price %>"
            />
            <div class="invalid-feedback">
              Поле должно быть заполнено в такой форме: 2324.30
            </div>
          </div>

          <div class="mt-3">
            <button class="btn btn-primary button" type="button" id="btn">
              Добавить
            </button>
            <button class="hidden" type="submit">Добавить</button>
            <a href="/products" class="btn btn-primary">Назад</a>
          </div>
        </form>
      </main>
    </div>
    <%- include('../partials/scripts.ejs') %>
  </body>
</html>
